# This is a Databricks asset bundle definition for bundle_tutorial.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bundle_tutorial
  uuid: 4b793a48-c630-4816-aa69-f242ec1842f8

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1546103163876983.3.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-1546103163876983.3.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/camilo.velez@bpt.com.co to make sure we only have a single copy.
      root_path: /Workspace/Users/camilo.velez@bpt.com.co/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: camilo.velez@bpt.com.co
        level: CAN_MANAGE
